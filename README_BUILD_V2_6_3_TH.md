# GoloPine DSP 7CH v2.6.3

แก้ Build จาก v2.6.2 โดยลดชุดเครื่องมือเป็น AGP 8.6.1 + Gradle 8.7 + Java 17 และเพิ่ม compileOptions Java 17

หน้า MAIN และ EQ 15-Band ยังคงเดิม รวมปุ่มเชื่อมต่อ/ตัดการเชื่อมต่อ DSP

ถ้า build ล้ม workflow จะแสดงเฉพาะบรรทัด error สำคัญและ 220 บรรทัดท้ายของ build.log
